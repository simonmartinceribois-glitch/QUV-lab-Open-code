# SPEC — fix/wizard-step1-labels (LOW, base `develop`)

> Demande : assistant étape 1 — bannière info invisible ; `Titre descriptif` → `Projet` ;
> `Projet / Client / Demandeur` → `Client` ; `Notes générales` → `Descriptif`
> (titres seuls, placeholders et données inchangés).

## Tests

`typecheck` (0), `npm test` (195/195), `build` OK + visuel wizard étape 1.
