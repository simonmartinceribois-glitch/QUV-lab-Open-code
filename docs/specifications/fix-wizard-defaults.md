# SPEC — fix/wizard-defaults (LOW, base `develop`)

> Demande : étape 3 — essence `Pin sylvestre standardisé` → `Pin sylvestre`,
> fabricant `Laboratoire / Fabricant` → `fabricant`, label `Fabricant / Fournisseur` →
> `Fabricant de la finition`, méthode `Pinceau` → `Airmix` (défauts de création + 1 libellé).

## Tests

`typecheck` (0), `npm test` (195/195), `build` OK + visuel étape 3.
Note : données démo/existantes intactes (défauts de création uniquement).
