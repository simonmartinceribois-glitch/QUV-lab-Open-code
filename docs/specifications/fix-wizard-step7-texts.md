# SPEC — fix/wizard-step7-texts (LOW, base `develop`)

> Demande : étape 7 sans bannière ni encadré adhérence ni ligne Fil ; `sans arrêt paillasse`
> → `sans arrêt pour mesurage` (étapes 6+7, cohérence) ; `(N familles actives)` →
> `(N types de mesures)`.

## Tests

`typecheck` (0), `npm test` (195/195), `build` OK + visuel étapes 6-7.
